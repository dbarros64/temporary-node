// GLOBAL VARIABLES -- NO WINDOW

// __dirname = path to current directory

// __filename = the filename

// require = function to use modules (Common JS)

// module = info about the current module  (WE WILL COVER THIS AND REQUIRE COMING UP SOON).

// process = info about the environment where the program is being executed.----- VERY USEFUL

// console.log(__dirname);
// console.log(__filename);

setInterval(() => {
  console.log("hello world");
}, 1000);
